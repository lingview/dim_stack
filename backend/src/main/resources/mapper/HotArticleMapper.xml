<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="xyz.lingview.dimstack.mapper.HotArticleMapper">

    <resultMap id="hotArticleMap" type="xyz.lingview.dimstack.dto.HotArticleDTO">
        <id property="id" column="id"/>
        <result property="article_id" column="article_id"/>
        <result property="title" column="title"/>
        <result property="excerpt" column="excerpt"/>
        <result property="image" column="image"/>
        <result property="date" column="date"/>
        <result property="author" column="author"/>
        <result property="category" column="category"/>
        <result property="alias" column="alias"/>
    </resultMap>

    <!-- 查询阅读量最高的10篇文章 -->
    <select id="selectHotArticles" resultMap="hotArticleMap">
        SELECT
        a.id,
        a.article_id,
        a.article_name AS title,
        a.excerpt,
        a.article_cover AS image,
        a.create_time AS date,
        u.username AS author,
        a.category,
        a.alias
        FROM article a
        JOIN user_information u ON a.uuid = u.uuid
        WHERE a.status = 1
        ORDER BY a.page_views DESC
        LIMIT 10
    </select>

</mapper>
