<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="xyz.lingview.dimstack.mapper.HotArticleMapper">


    <!-- 查询阅读量最高的10篇文章 -->
    <select id="selectHotArticles" resultType="xyz.lingview.dimstack.dto.request.HotArticleDTO">
        SELECT
            a.id,
            a.article_id,
            a.article_name AS title,
            a.excerpt,
            a.create_time AS date,
            u.username AS author,
            a.category,
            a.page_views,
            a.alias
        FROM article a
                 INNER JOIN user_information u ON a.uuid = u.uuid
        WHERE a.status = 1
        ORDER BY a.page_views DESC
        LIMIT 10;
    </select>

</mapper>
