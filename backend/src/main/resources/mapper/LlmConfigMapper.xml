<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xyz.lingview.dimstack.mapper.LlmConfigMapper">

    <select id="getLlmConfig" resultType="xyz.lingview.dimstack.domain.LlmConfig">
        SELECT
            api_key,
            api_url,
            model
        FROM llm_config
        LIMIT 1
    </select>

    <update id="updateLlmConfig" parameterType="xyz.lingview.dimstack.domain.LlmConfig">
        UPDATE llm_config
        SET api_key = #{api_key},
            api_url = #{api_url},
            model = #{model}
    </update>

    <insert id="insertLlmConfig" parameterType="xyz.lingview.dimstack.domain.LlmConfig">
        INSERT INTO llm_config (api_key, api_url, model)
        VALUES (#{api_key}, #{api_url}, #{model})
    </insert>
</mapper>
