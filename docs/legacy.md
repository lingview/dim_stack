# 老版本相关说明

## 数据库迁移

> v84+ 版本已支持一键更新系统，可忽略此节

v84 以下的版本没有一键更新系统功能，需要手动执行数据库更新脚本。

### 需要手动升级的版本

以下版本升级需要手动执行数据库升级脚本（如果数据库不是默认名 `dim_stack`，请将脚本中的 `USE dim_stack;` 修改为对应的数据库名）：

- **v54 → v55+**
- **v64 → v65+**
- **v77 → v84+**

请到 `数据库更新脚本` 目录中下载对应的升级脚本，并根据自己的数据库版本选择 mysql5 或 mysql8 脚本。
