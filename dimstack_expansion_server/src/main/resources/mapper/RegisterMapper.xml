<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xyz.lingview.dimstack_expansion_server.mapper.RegisterMapper">
    <resultMap type="xyz.lingview.dimstack_expansion_server.domain.Register" id="Register">
        <id property="uuid" column="uuid" />
        <result property="username" column="username" />
        <result property="phone" column="phone" />
        <result property="email" column="email" />
        <result property="password" column="password" />
        <result property="role_id" column="role_id" />
        <result property="status" column="status" />
    </resultMap>

    <select id="selectUser" resultType="int">
        SELECT
            CASE WHEN EXISTS (
                SELECT 1 FROM user_information
                WHERE username = #{username}
            ) THEN 1 ELSE 0 END AS exists_flag
    </select>

    <insert id="insertUser" parameterType="xyz.lingview.dimstack_expansion_server.domain.Register">
        INSERT INTO user_information(uuid, username, phone, email, password, role_id, status)
        VALUES (#{uuid} ,#{username}, #{phone}, #{email}, #{password}, #{role_id}, 1)
    </insert>

</mapper>